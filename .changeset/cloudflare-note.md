---
"@geminixiang/jev": patch
---

Document that the `cloudflare` provider is unverified: `typesafe/jev` is not present in Cloudflare Workers AI's model catalog as of 2026-09, despite an indexed docs page describing it. The implementation is unchanged and ready for when/if the model ships.
